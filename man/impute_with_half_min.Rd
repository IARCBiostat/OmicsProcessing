% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_outliers.R
\name{impute_with_half_min}
\alias{impute_with_half_min}
\title{Impute Missing Values with Half the Minimum}
\usage{
impute_with_half_min(df, cols = NULL)
}
\arguments{
\item{df}{A data frame containing numeric columns to impute.}

\item{cols}{Optional. A character vector of column names to impute. If \code{NULL}, all numeric columns are imputed.}
}
\value{
A data frame with missing values imputed in specified (or all numeric) columns.
}
\description{
This function imputes missing values in numeric columns by replacing each \code{NA}
with half the minimum non-missing value of that column. You can restrict imputation
to a subset of columns using the \code{cols} argument.
}
\examples{
\dontrun{
impute_with_half_min(df) # Impute all numeric columns
impute_with_half_min(df, cols = c("a", "b")) # Impute only columns a and b
}
}
