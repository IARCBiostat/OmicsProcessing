% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_normalization_comparison.R
\name{plot_normalization_comparison}
\alias{plot_normalization_comparison}
\title{Plot feature values before/after normalization by run order}
\usage{
plot_normalization_comparison(
  df_before,
  df_after = NULL,
  target_cols,
  is_qc_before,
  is_qc_after = NULL,
  batch = NULL,
  plate = NULL,
  run_order,
  title_before = "Before normalization",
  title_after = "After normalization"
)
}
\arguments{
\item{df_before}{A data frame containing the data \strong{before preprocessing}.
Must include the columns referenced by \code{target_cols} and the column named in
\code{run_order}. Optionally includes columns named in \code{batch} and/or \code{plate}.}

\item{df_after}{An optional data frame containing the data \strong{after preprocessing}.
If provided, must include the columns referenced by \code{target_cols} and the column named
in \code{run_order}. Optionally includes columns named in \code{batch} and/or \code{plate}. Default: \code{NULL}.}

\item{target_cols}{Feature columns to normalize. Character vector or selector
passed to \code{resolve_target_cols}). These columns are pivoted to long format and faceted
(one facet per feature).}

\item{is_qc_before}{Logical vector (length \code{nrow(df_before)}) indicating which rows are
QC samples (\code{TRUE}) vs. regular samples (\code{FALSE}) in the data before preprocessing.
Used to control point shape and size.}

\item{is_qc_after}{Optional logical vector (length \code{nrow(df_before)}) indicating which rows are
QC samples (\code{TRUE}) vs. regular samples (\code{FALSE}) in the data after preprocessing.
Required if \code{df_after} is provided. Default: \code{NULL}.}

\item{batch}{Optional string giving the \strong{name of the column} in \code{df_before}/\code{df_after}
that encodes batch membership (e.g., \code{"batch_id"}). If provided, vertical dotted
lines mark batch boundaries (computed from the maximum run order per batch).
If \code{NULL}, all samples are treated as a single batch and no boundary lines are drawn.
Default: \code{NULL}.}

\item{plate}{Optional string giving the \strong{name of the column} in \code{df_before}/\code{df_after}
that encodes plate ID. If provided, points are colored by plate. If \code{NULL},
all points use a single color label \code{"all"}. Default: \code{NULL}.}

\item{run_order}{String giving the \strong{name of the column} that encodes the
run order. This column is coerced to numeric and used as the x‑axis,
and the data are sorted by this column prior to plotting.}

\item{title_before}{Title for the "before" panel/plot. Default: \code{"Before normalization"}.}

\item{title_after}{Title for the "after" panel/plot. Default: \code{"After normalization"}.}
}
\value{
Invisibly returns a \strong{patchwork/ggplot object}:
\itemize{
\item If \code{df_after} is \code{NULL}: a single plot for the "before" data (with legend).
\item Otherwise: a two‑panel (before | after) plot with a shared legend on the right.
}
}
\description{
This function visualizes the effect of normalization (e.g., SERRF) on
omics feature values across batches and plates.  It creates scatter plots of feature values
over \strong{run order} for a set of target features, highlighting QC vs. non‑QC sample,
optionally with batch boundaries indicated by vertical dotted lines and points colored by plate.
If \code{df_after} is supplied, the function returns a side‑by‑side comparison (before vs. after normalization)
with a shared legend. Otherwise, it returns only the "before" plot.
}
\details{
Internally, the function:
\itemize{
\item Selects columns \verb{\{batch, plate, run_order\} ∪ target_cols} if present (using
\code{dplyr::any_of} for optional columns and \code{dplyr::all_of} for required features).
\item Adds an \code{is_qc} flag and pivots features to long format (\code{feature}, \code{value}).
\item Coerces \code{run_order} to numeric and orders rows accordingly.
\item Creates a scatter plot (\code{value} vs. \code{run_order}), faceted by \code{feature}
(\code{scales = "free_y"}), with:
\itemize{
\item Color mapped to \code{plate} (rainbow palette if multiple plates; grey if single plate),
\item Shape and size mapped to \code{is_qc} (QC = filled circle, larger; Sample = triangle, smaller),
\item Optional vertical dotted lines at batch boundaries (end of each batch).
}
\item If \code{df_after} is provided, returns a side‑by‑side comparison (before | after)
using \code{patchwork::plot_layout(guides = "collect")} to share the legend.
}

\strong{Inputs as column names (strings):} \code{batch}, \code{plate}, and \code{run_order} are
expected to be \strong{character scalars naming columns} in the data frames, not vectors.

\strong{Optional columns:} If \code{batch} or \code{plate} is \code{NULL}, a placeholder factor
\code{"all"} is used; if \code{batch} is \code{NULL}, no boundary lines are drawn.
}
