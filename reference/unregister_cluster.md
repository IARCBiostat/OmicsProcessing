# Unregister a parallel cluster used for parallel processing.

This function unregisters a previously registered parallel cluster used
for parallel processing in R.

## Usage

``` r
unregister_cluster(cluster = NULL)
```

## Arguments

- cluster:

  The parallel cluster object to be unregistered (default is NULL).
